key.txt.age

# Git
{{- if not (default false .gitconfig.cw.enable) }}
.gitconfig-cw
{{- end }}
{{- if not (default false .gitconfig.quintegro.enable) }}
.gitconfig-quintegro
{{- end }}

# Keep only for toaster mac
{{- if ne .chezmoi.hostname "toaster" }}
.hammerspoon
.aerospace.toml
.config/karabiner/
.config/mise/config.toml
.config/ghostty/config
{{- end }}
