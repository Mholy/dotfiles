key.txt.age

{{- if not (default false .gitconfig.cw.enable) }}
.gitconfig-cw
{{- end }}
{{- if not (default false .gitconfig.quintegro.enable) }}
.gitconfig-quintegro
{{- end }}

{{- if eq .chezmoi.hostname "toaster" }}
# Only for toaster
.hammerspoon
.aerospace.toml
cli/,ccc-reset.sh
cli/,mount-toaster-backup.sh
.config/karabiner/
.config/mise/config.toml
.config/starship.toml
{{- end }}
